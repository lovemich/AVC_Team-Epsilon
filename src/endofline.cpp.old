#include "extern.h"

//take_picture();
int redCheck = get_pixel(IMAGE_SIZE_X / 2 + i, IMAGE_SIZE_Y / 2, 0);
int greenCheck = get_pixel(IMAGE_SIZE_X / 2 + i, IMAGE_SIZE_Y / 2, 1);
int blueCheck = get_pixel(IMAGE_SIZE_X / 2 + i, IMAGE_SIZE_Y / 2, 2);

//if((redCheck > 200) && (greenCheck < 80) && (blueCheck < 80) && (read_analog(1) < 100) && (read_analog(2) < 100)){
//  break;
//  }
